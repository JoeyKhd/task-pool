{
  "name": "task-pool",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "node ./lib/index.js",
    "tsc:build": "tsc -p ./tsconfig.build.json",
    "tsc:build:watch": "tsc -w -p ./tsconfig.build.json",
    "tsc:alias": "tsc-alias -p ./tsconfig.build.json",
    "tsc:alias:watch": "tsc-alias -w -p ./tsconfig.build.json"
  },
  "devDependencies": {
    "@types/node": "^16.9.4",
    "eslint": "^8.23.0",
    "ts-node": ">=8.0.0",
    "tsc-alias": "^1.7.0",
    "typechain": "^8.1.0",
    "typescript": "^4.8.3"
  },
  "packageManager": "yarn@3.2.4",
  "dependencies": {
    "redis": "^4.5.0"
  }
}
